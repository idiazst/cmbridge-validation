# cmbridge-validation

Development and validation repository for the `cmbridge` R package.

The immediate goals are:

- implement conditional-moment bridge estimators in R;
- validate sieve minimum-distance, Landweber, and PMMR implementations;
- run large-sample simulation diagnostics;
- run `R CMD check` in GitHub Actions.
