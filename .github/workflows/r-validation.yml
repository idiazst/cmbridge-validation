name: R validation

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      R_KEEP_PKG_SOURCE: yes
      CMBRIDGE_VALIDATION_DIR: validation-artifacts

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up R
        uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - name: Install package dependencies
        uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: |
            any::rcmdcheck
          needs: check

      - name: R CMD check
        shell: Rscript {0}
        run: |
          rcmdcheck::rcmdcheck(
            args = c("--no-manual", "--as-cran"),
            error_on = "error",
            check_dir = "check"
          )

      - name: Install cmbridge
        run: R CMD INSTALL .

      - name: Run large-sample validation in R
        run: Rscript inst/simulations/validate_large_sample.R

      - name: Show validation results
        if: always()
        run: |
          if [ -f validation-artifacts/validation_results.csv ]; then
            cat validation-artifacts/validation_results.csv
          fi

      - name: Upload validation artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cmbridge-r-validation
          path: |
            validation-artifacts/
            check/
          if-no-files-found: warn
